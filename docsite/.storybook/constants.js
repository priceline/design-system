/**
 * Reusable objects to make documenting your components with storybook's info addon a little easier.
 *
 * Everyone uses storybook a little differently so you might not even need these.
 */

const defaultParameters = { info: { disable: true } }
const documentationConfig = { info: { disable: false, inline: true } }

export { defaultParameters, documentationConfig }
