import { Canvas, Meta, Story, ArgsTable } from '@storybook/addon-docs'
import styled from 'styled-components'
import { Button } from '.'
import { ForwardRefs } from './ForwardRefsStory'
import { argTypes } from './Button.stories.args'
import { Hero, TableOfContents, TextSection } from '../storybook/components'
import heroImage from './Button.Hero.png'
import { Text, ThemeProvider } from '..'

export const Template = (args) => <Button {...args}>I AM BUTTON</Button>

export const name = 'Button'
export const desc =
  'Buttons allow users to trigger an action or event with a single click. For example, you can use a button for allowing the functionality of submitting a form, opening a dialog, canceling an action, or performing a delete operation.'
export const links = {
  overview: 'Overview',
  props: 'Props',
  usage: 'Usage',
}

<Meta title='core/Button' component={Button} argTypes={argTypes} />

<Hero name={name} desc={desc} img={heroImage} />
<TableOfContents links={links} />

<hr />

<ArgsTable story={name} />

<Canvas>
  <Story name='Button'>{Template.bind({})}</Story>
</Canvas>

<Canvas>
  <Story
    name='Responsive size'
    args={{ size: ['small', null, 'medium', null, 'large'] }}
    parameters={{
      viewport: { defaultViewport: 'designSystem_sm' },
    }}
  >
    {Template.bind({})}
  </Story>
</Canvas>
