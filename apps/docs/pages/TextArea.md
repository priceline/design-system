# TextArea

The `TextArea` component is a styled `<textarea>` element.

```.jsx
<TextArea
  id='foo'
  name='foo'
  defaultValue='foo'
/>
```

```.jsx
<Label htmlFor='bar'>Bar</Label>
<TextArea
  id='bar'
  name='bar'
  defaultValue='bar'
/>
```
